# 运行指导

要运行本程序，需要提前安装Gurobi并配置好环境。在终端输入：gurobi_cl --version，如果正确显示说明配置成功。

配置Gurobi环境后，在exact_solver/cmake/FindGUROBI.cmake中把所有的/opt/gurobi1202替换为自己的gurobi路径。

使用Cmake配置并构建可执行文件，在main函数开头可以更改所有需要的文件路径和超参数。